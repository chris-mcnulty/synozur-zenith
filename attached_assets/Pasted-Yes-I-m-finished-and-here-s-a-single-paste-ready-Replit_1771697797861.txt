Yes — I’m finished, and here’s a single paste-ready Replit prompt that will generate a working Zenith MVP with the right Vega/Constellation-style splash + Entra SSO flow, multi-tenancy, service plans, Purview/metadata governance scaffolding, and the hard “NO POLLING” rule baked in. This prompt follows the same “vibecoding” approach you’ve used with Replit before. 1234

✅ Replit Prompt (copy/paste into Replit Ghostwriter / Agent)
You are Replit Ghostwriter (senior full-stack engineer + UX-minded product builder).
Build a production-like MVP SaaS web app named “Zenith” for Microsoft 365 governance and provisioning.
The MVP must feel like a paid SaaS product (even if no billing), with service plans, tenant isolation, and enterprise SSO.

========================================================
0) HARD CONSTRAINTS (DO NOT VIOLATE)
========================================================
A) NO POLLING FOR M365 DATA
- Do NOT implement cron jobs, periodic scans, or background loops that enumerate Microsoft 365 objects.
- All “M365 state” updates must be event-driven or intent-driven:
  1) Event-driven: webhook receiver endpoints that accept events (mock Microsoft Graph change notifications).
  2) Intent-driven: user clicks an action (e.g., “Create Team”) and Zenith pulls/uses only the minimum required data for that transaction (in this MVP, mock the Graph call).
- For “Enterprise+ bulk analysis (MGDC/file share analysis)”, implement as explicit, user-initiated batch jobs only (manual run button), not scheduled.

B) GOVERNANCE, NOT MONITORING
- Do not build health monitoring, performance metrics, user activity analytics, or experience scoring.

C) SECURITY
- Do not hardcode secrets in code.
- Use environment variables for all secrets and tenant IDs.

========================================================
1) TECH STACK (MVP FRIENDLY, REPLIT COMPATIBLE)
========================================================
- TypeScript end-to-end
- Next.js (App Router) for UI + API routes
- Tailwind CSS for styling (Vega/Constellation-like clean SaaS UI)
- SQLite (via Prisma) for fast Replit DB (MVP). Use tenantId on every row.
- Authentication: Microsoft Entra ID OIDC using NextAuth (or a lightweight OIDC implementation if easier).
- RBAC in-app: roles stored in DB, enforced in middleware.
- Feature gating by “service plan” stored in DB.
- Include dark mode toggle.
- Include a small seeded dataset for demo.

========================================================
2) IA / UX REQUIREMENTS (VEGA + CONSTELLATION INSPIRED)
========================================================
A) Public splash homepage (/)
- A “splashy” hero page with a single primary CTA: “Sign in with Microsoft”.
- Sections: Value prop, capabilities, service plans, security/trust, footer.
- Minimal top nav: Product, Plans, Security, Docs, Sign in.

B) Login flow
- Clicking sign-in sends to Entra login.
- Callback route: /auth/entra/callback (implement using NextAuth route handling).
- After login, route to /app with a tenant resolver:
  - If user belongs to 1 Zenith tenant: route to /app/dashboard.
  - If >1: show tenant picker /app/select-tenant.
  - If none: show /app/no-access with “request access” message.

C) Mainline app shell (/app/*)
- Left nav: Dashboard, Provision, Governance, Purview, Lifecycle, Reports, Admin.
- Top bar: tenant + environment selector, search box, user menu.

D) Constellation-like “self-documenting” patterns
- Persistent “User Guide” link (lower-left)
- Persistent “Help” button (lower-right) that opens a simple drawer (no ticketing integration required).

========================================================
3) CORE DOMAIN MODEL (DATABASE SCHEMA)
========================================================
Create Prisma models with these entities (minimum fields shown; add what you need):

Tenant
- id (uuid)
- name
- plan (enum: TRIAL, FREE, BASE, ENTERPRISE, UNLIMITED)
- createdAt

Environment
- id
- tenantId
- name (e.g., Prod, Test, Sandbox)
- m365TenantId (string)
- cloud (enum: COMMERCIAL, GCC, GCC_HIGH, DOD, OTHER)
- createdAt

User
- id
- email
- displayName
- createdAt

Membership
- id
- tenantId
- userId
- role (enum: GLOBAL_ADMIN, TENANT_ADMIN, ENV_ADMIN, SERVICE_OWNER, BUSINESS_REQUESTOR, EXEC_VIEWER)
- createdAt

WorkspaceRequest
- id
- tenantId
- environmentId
- requestedByUserId
- type (enum: TEAM, SHAREPOINT_SITE, M365_GROUP)
- displayName
- purpose
- sensitivity (enum: PUBLIC, INTERNAL, CONFIDENTIAL, HIGHLY_CONFIDENTIAL)
- externalSharingIntent (boolean)
- status (enum: DRAFT, SUBMITTED, APPROVED, REJECTED, PROVISIONED)
- createdAt

Workspace
- id
- tenantId
- environmentId
- type
- displayName
- m365ObjectId (string, nullable for mock)
- sensitivity
- ownersJson (string)
- metadataJson (string)  // “properly bags” + Teams metadata mirror
- lifecycleState (enum: ACTIVE, REVIEW, RENEWAL_DUE, ARCHIVED, RETAINED, DELETED)
- createdAt

Policy
- id
- tenantId
- environmentId (nullable, means inherited)
- policyType (enum: NAMING, METADATA_REQUIRED, EXTERNAL_SHARING, LIFECYCLE_DEFAULTS, COPILOT_ELIGIBILITY)
- version (int)
- rulesJson (string)
- isActive (boolean)
- createdAt

PurviewConfig
- id
- tenantId
- environmentId
- configType (enum: SENSITIVITY_LABELS, DLP_ADAPTIVE_SCOPES, RETENTION, RECORDS)
- configJson (string)
- status (enum: DRAFT, PUBLISHED)
- createdAt

EventIngest
- id
- tenantId
- environmentId
- eventType (string)
- source (enum: GRAPH, MANUAL, SYSTEM)
- payloadJson
- processed (boolean)
- createdAt

AuditLog
- id
- tenantId
- environmentId (nullable)
- actorUserId (nullable)
- actorType (enum: USER, SYSTEM, AGENT)
- action (string)
- targetType (string)
- targetId (string)
- detailJson
- createdAt

BatchJob  // Enterprise+ only
- id
- tenantId
- environmentId
- jobType (enum: MGDC_EXPORT, FILESHARE_DISCOVERY)
- status (enum: QUEUED, RUNNING, SUCCEEDED, FAILED)
- resultJson
- createdAt

========================================================
4) FEATURE GATING (SERVICE PLANS)
========================================================
Implement plan gates:
- TRIAL/FREE/BASE: Core governance, provisioning, metadata governance, Purview config screens (BASE+), lifecycle, reports (basic).
- ENTERPRISE/UNLIMITED: enable Batch Jobs (MGDC_EXPORT + FILESHARE_DISCOVERY) and multi-environment governance tooling.
- UNLIMITED: allow custom policy builder (just a UI toggle + JSON editor is fine for MVP).

Block gated features gracefully:
- Show a lock icon + short message + “Contact admin”.

========================================================
5) KEY FLOWS (MVP IMPLEMENTATION)
========================================================
A) Provisioning
- /app/provision/new
  - Form captures name, purpose, type, sensitivity, externalSharingIntent
  - Validate naming policy and metadata required policy
  - Submit creates WorkspaceRequest with status SUBMITTED
- /app/provision/approvals (role gated)
  - Approve/Reject
  - Approve triggers a mocked “provision” action:
    - Create Workspace record with m365ObjectId = “mock-<uuid>”
    - Write AuditLog entries
  - IMPORTANT: Provisioning is user/approval initiated (intent-driven), not polling.

B) Metadata Governance
- /app/governance/workspaces/[id]
  - Show Workspace metadataJson editor (structured UI + raw JSON tab)
  - “Apply required metadata” button (writes to metadataJson and logs action)
  - Show a “Copilot eligibility” status that is derived from policy + sensitivity only (no telemetry).

C) Purview Config
- /app/purview
  - Tabs: Sensitivity Labels, Adaptive DLP Scopes, Retention, Records
  - Each tab is a JSON-backed editor in DB.
  - “Publish” changes status to PUBLISHED and logs AuditLog.
  - Do not call real Purview APIs in MVP; just structure the model as if it will.

D) Lifecycle
- /app/lifecycle
  - List workspaces with lifecycleState
  - Allow manual state transitions (Review → Archive → Retain)
  - Provide “Renewal due” as a calculated view (based on createdAt + policy default), computed on page load (not background jobs).

E) Reporting
- /app/reports
  - Governance summary: label coverage % (based on Workspace.sensitivity), metadata completeness (based on required keys), Copilot eligibility counts.
  - Export CSV button.

F) Events (NO POLLING)
- /api/events/graph (POST)
  - Accept mocked Graph events, store in EventIngest, process immediately, update Workspace if event references one.
- /app/admin/events
  - View event log + processed status.

G) Batch Jobs (Enterprise+ only)
- /app/admin/batch-jobs
  - Button “Run MGDC Export (Mock)”
  - Button “Run File Share Discovery (Mock)”
  - Running creates BatchJob and simulates progress (in-request or via queued in-memory worker), but MUST be explicitly user initiated.

========================================================
6) RBAC & AUTHORIZATION
========================================================
- Enforce tenant membership required for /app routes.
- Enforce role checks:
  - TENANT_ADMIN: policies, purview configs, reports, approvals
  - ENV_ADMIN: environment settings
  - EXEC_VIEWER: read-only dashboards and reports
  - BUSINESS_REQUESTOR: can submit WorkspaceRequests
- Add middleware to verify:
  1) authenticated
  2) tenant selected
  3) role authorization

========================================================
7) UI DETAILS (SaaS polish)
========================================================
- Use clean “cards + tabs + left nav” layout.
- Provide toast notifications for actions.
- Provide empty-state illustrations (simple SVG).
- Include a “What’s New” modal on first login (dismissible).

========================================================
8) PROJECT OUTPUT
========================================================
Your output must include:
- Working app in Replit with run button
- README.md with:
  - how to set env vars for Entra (placeholders)
  - how to seed demo tenant/users
  - explanation of “NO POLLING” rule
- Seed data:
  - 2 tenants: “Synozur Demo (BASE)” and “Enterprise Demo (ENTERPRISE)”
  - 2 environments each (Prod/Test)
  - sample policies and workspaces

========================================================
9) ENV VARS (placeholders only)
========================================================
- ENTRA_CLIENT_ID=
- ENTRA_CLIENT_SECRET=
- ENTRA_TENANT_ID=
- NEXTAUTH_URL=
- NEXTAUTH_SECRET=
- DATABASE_URL=

Do not include actual values. Use placeholders in README.

Build this now.

Why this prompt is “on brand” for how you build
It mirrors how you’ve described vibecoding with Replit as a rapid path to a clickable product experience. 156 
It uses your documented Entra callback pattern (but safely as placeholders). 37 
It matches your shared-services + tenant/service plan approach in Galaxy v1 Speciification. 48 

Quick close
If you paste that into Replit, you’ll get a complete Zenith MVP with the correct governance-first posture and a baked-in NO POLLING architecture. If you want, I can also generate a second prompt that upgrades the MVP into a multi-service “Galaxy-connected” variant (auth/tenant/plan as a shared module) using your Galaxy v1 Speciification patterns. 4
